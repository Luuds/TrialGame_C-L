using UnityEngine;
using System.Collections;

public class BackgroundBehavior : MonoBehaviour {

	// Use this for initialization
	void Start () {
		/*
		gameObject.transform.rotation = new Vector2(Random.Range (-5f, 5f), Random.Range (-5f, 5f));
		float tiltAngle = Random.Range(0f, 360f);
		Quaternion target = Quaternion.Euler (tiltAngle, tiltAngle, tiltAngle);
		transform.rotation = Quaternion.Slerp (transform.rotation, target, 0);
		*/
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
